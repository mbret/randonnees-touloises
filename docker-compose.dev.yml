services:
  postgres:
    image: postgres:16
    ports:
      - '54320:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: db
      POSTGRES_PASSWORD: password
      POSTGRES_HOST_AUTH_METHOD: trust
    env_file:
      - .env
